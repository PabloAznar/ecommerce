server:
  port: 8080

spring:
  application:
    name: ecommerce
  datasource:
    url: jdbc:h2:mem:mydb
    username: sa
    password: password
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
endpoints:
  base: /api/v1
